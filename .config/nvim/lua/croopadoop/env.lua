vim.g.env = {
	-- swap files are stored here
	tempdir = false,

	-- ensure cursor retains "beam" shape
	preserve_beam_cursor = false,

	-- number of lines for "small" windows
	small_screen = 19,

	-- path to project local settings folder
	project_store = nil
}
